# Project Rules

- After changing files in this repository, commit the changes and push them to the configured remote unless the user explicitly says not to.
- If push is blocked by authentication, permissions, or network problems, report the blocker clearly and leave the local commit ready to push.
